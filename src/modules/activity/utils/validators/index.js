const validateCreateActivity = require('./createActivity.validator');
const validateListActivities = require('./listActivities.validator');

module.exports = {
  validateCreateActivity,
  validateListActivities
};
